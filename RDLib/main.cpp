#include "stdafx.h"
#include "RDRegister.h"

int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, PSTR szCmdLine, int iCmdShow)
{
	/*
	char * path[] = { "Software", "RDesktop", 0};
	RDRegister reg;
	int i=0;
	char *text;
	reg.setBaseKey();
	if ( reg.openTree(path, true) ) {
		reg.readString("DD", text);
		delete [] text;
	}*/

	return 0;
}